<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--  Permissão necessária para enviar notificações no Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!--  Permissão para receber o evento de boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_gota"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_gota"
        android:supportsRtl="true"
        android:theme="@style/Theme.BebaAgua"
        tools:targetApi="33">


        <!--  **Boot Receiver** -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 🔹 **BroadcastReceiver** -->
        <receiver
            android:name=".LembreteReceiver"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="beba.agua.LembreteReceiver" />
            </intent-filter>
        </receiver>

        <!-- Tela de lembretes -->
        <activity
            android:name=".LembretesActivity"
            android:exported="false" />

        <!-- Atividade principal (Tela Inicial) -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
